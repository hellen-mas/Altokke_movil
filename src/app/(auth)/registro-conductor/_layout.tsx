import { Stack } from "expo-router";

export default function RegistroConductorLayout() {
    return (
        <Stack
            screenOptions={{
                headerShown: false,
            }}
        />
    );
}